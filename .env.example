# 复制为仓库根 .env 后编辑（.env 已 gitignore，本示例可提交）。
#
# 用途：nb / pip 安装的 NoneBot 插件环境变量（Pallas 主仓插件请优先用 WebUI 或 webui.json）。
# 监听、数据库、超管写在 config/pallas.toml（见 config/pallas.example.toml）。
#
# 合并顺序（后者覆盖前者）：pallas.toml → .env → .env.{ENVIRONMENT} → webui.json
# 同名键请只保留一处，避免与 WebUI 落盘冲突。
#
# pip 插件安装后还须注册 pyproject.toml，否则不会加载：
#   uv add nonebot-plugin-xxx
#   [tool.nonebot.plugins]
#   nonebot-plugin-xxx = ["nonebot_plugin_xxx"]
#
# ---- 按插件文档追加键名（示例占位）----
# SOME_NB_PLUGIN_OPTION=
