# Pallas-Bot 官方扩展包模板

复制为独立仓库 `pallas-plugin-<name>`，用于新建 4.0 官方扩展。安装：`uv sync --extra plugins-<name>` 或 pip。

README 模板见 [`readmes/`](readmes/)（手工复制到扩展仓；已升格 core 的插件如 `llm_chat`、`pb_stats` 不再维护 pip 仓）。
